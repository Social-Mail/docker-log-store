# docker-log-store
Centralized Docker Log Store using Postgres with Auto Clean up 

## Log Server
Runs somewhere with dedicated connection to postgres (even self installed) written in nodejs.

## Log Agent
Runs on a host where docker is installed, written in dotnet.